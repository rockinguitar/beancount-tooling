SELECT
  date AS "Datum",
  narration AS "Buchungstext",
  abs(position) as "Betrag"
FROM postings
WHERE account ~ '^Income:'
  AND abs(number(units(position))) > 0
{{- if .From }}
  AND date >= {{ .From }}
{{- end }}
{{- if .To }}
  AND date <= {{ .To }}
{{- end }}
ORDER BY date;
